<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.BoardMapper">
 	<select id="select"  resultType="vo.BoardVO">
    	select board_id, title, content, to_char(writedate,'rrrr-mm-dd') writedate, cnt, writer from board
 	</select>
 	 <select id="read" resultType="vo.BoardVO">
    	select board_id, title, content, to_char(writedate,'rrrr-mm-dd') writedate, cnt, writer from board where board_id = #{board_id}
 	</select>
 	<update id="Cntup" parameterType="vo.BoardVO">
 				update board set
 				cnt = cnt + 1
 				where board_id = #{board_id}
 	</update>
 	
 	<select id="selectWriter" parameterType="string" resultType="vo.BoardVO">
    	select board_id, title, content, to_char(writedate,'rrrr-mm-dd') writedate, cnt, writer from board where writer = #{writer}
 	</select>
  	<insert id="insert"  parameterType="vo.BoardVO">
  		<selectKey resultType="int" keyProperty="board_id" order="BEFORE">
  			select board_seq.nextval from dual
   		</selectKey>
  		 insert into board (board_id , title , content, writedate, cnt, writer) values(#{board_id}, #{title} , #{content} ,to_date(sysdate,'rrrr-mm-dd'), 0, #{writer} )
    </insert>
	<select id="search" parameterType="java.util.HashMap" resultType= "vo.BoardVO">
    	select board_id, title, content, to_char(writedate,'rrrr-mm-dd') writedate, cnt, writer 
    	         from board where ${searchType} like '%'||#{keyword}||'%'
    </select>
    <delete id="delete" parameterType="_int">
    	delete from board where board_id = #{board_id}
    </delete>
    <update id="update" parameterType="vo.BoardVO">
		update board set title = #{title}, content= #{content}, writer = #{writer}
			where board_id = #{board_id}
    </update>   
</mapper>
